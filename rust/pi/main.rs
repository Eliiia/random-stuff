fn main() {

    let pi = 3.141592;

    // add : to modify it, i think?
    // add . to cut it, assuming its a number
    // add a number (in this case 3) or a * or some other stuff 
    println!("pi is roughly {:.3}", pi);
    

    
}